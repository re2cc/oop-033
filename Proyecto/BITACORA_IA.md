Should methods in Java class return List or ArrayList?

Aparentemente se considera buena practica retornar List en lugar de una implementacion concreta porque en realidad no importa la implementacion por detras, si no la funcionalidad, ademas de que permite cambiar la implementacion interna sin causar efectos secundarios en el resto del codigo.

How can I verify a card is 16 numbers long in Java?

Basicamente usando Regex

What is this error? Caused by: java.lang.IllegalStateException: Location is not set. (extra context)

Es un error causado por no incluir los archivos FXML como porte del codigo, basicamente habia que agregar unas lineas en el `build.gradle.kts` para incluirlos. 

How to build a jar using gradle?

Basicamente me dio un bloque de codigo que adapte a mi `build.gradle.kts`
