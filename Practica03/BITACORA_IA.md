- What does protected clases do?

En pocas palabras, en este caso nada porque como están dentro del mismo paquete, son practicamente lo mismo que un
atributo public.

- How can I create a unit test for this code (Showcase.class). Lets say I want to test the capacity check... Btw, how
does this test directory work? IDEA made it but it basically stops at test/java and I dont see a obvious button to make
a test from a class

Genero un test que hace exactamente lo que quería, no tengo ningún método que mida el tamaño del ArrayList de en un momento
dado asi que lo que hizo fue intentar un 6to objeto, que fallo, luego lo busco y como no esta, asume que funciona.
Ademas IDEA si tiene una forma simple de insertar test, Ctrl + Shift + T
